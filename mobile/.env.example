# Base URL of the Guardians LMS API.
# Leave unset in local development and the app will talk to the machine running
# `expo start` on port 4000, which is what a phone on the same Wi-Fi needs.
EXPO_PUBLIC_API_URL=http://192.168.1.10:4000
